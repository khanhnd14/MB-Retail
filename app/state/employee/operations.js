import {
  completeRegister,
  getBalance,
  getInfo,
  sendOtp,
  transfer,
  getRolloutAcc,
  deregister,
} from './actions'

export { completeRegister, getBalance, getInfo, sendOtp, transfer, getRolloutAcc, deregister }
